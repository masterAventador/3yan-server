spring:
  jpa:
    # 本机开发打开 SQL 日志，方便调试
    show-sql: true

# 注：sanyan.doubao / sanyan.llm.deepseek / sanyan.embedding.* 和 spring.datasource / spring.data.redis
# 全部继承 application.yml base 配置。本机 dev 启动靠 ~/.zshrc 中 export 的 3 个 API key 注入。
